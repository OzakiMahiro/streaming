resources:
  pipelines:
    lakeflow_declarative_pipelines:
      name: streaming_lakeflow_declarative_pipelines
      # src/streaming/common/config.py の CATALOG / SCHEMA_LDP と合わせる
      catalog: tech_survey
      # Jobs方式のbronze/silver/goldと書き込み先が衝突しないよう、LDP専用スキーマに出力する
      schema: ldp
      serverless: true
      libraries:
        - notebook:
            path: ../../src/notebooks/c_declarative_pipelines/08_lakeflow_declarative_pipelines.py
        - notebook:
            path: ../../src/notebooks/c_declarative_pipelines/09_data_quality_expectations.py
